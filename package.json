{
  "name": "Custom-UI-Reporter",
  "version": "1.0.0",
  "description": "Custom UI Reporter - Identifies custom UI files inside the SPM instance",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && mkdir build && cp ./configuration.json ./build && cp ./README.md ./build && webpack --config webpack.config.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "archiver": "^5.0.2",
    "cli-progress": "^3.8.2",
    "fs-extra": "^9.0.1",
    "glob": "^7.1.6",
    "xmldom": "^0.4.0"
  },
  "devDependencies": {
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}